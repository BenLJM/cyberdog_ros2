# CyberDog JP4→JP5 移植 · 交接文档（2026-07-26）

> 给新会话用。**先读这份 + `PORT-STATUS.md`**，再动手。
> `PORT-STATUS.md` 是权威现状（含部署铁律、⛔禁做清单、被推翻的旧结论）。

---

## 0. 一句话现状

> **JP5 内核（5.10.216-tegra / L4T r35.6.4）+ chroot 跑 JP4 出厂 ROS2 Foxy 栈**，
> 除 **AI 头顶相机采集** 外，全部主要元器件已打通并开机自愈。

当前内核 `b082b8ea` / DTB `2228d7ab`，tag `v1.0-ramoops-blackbox`，
狗上 `mi@10.0.0.219`（免密 sudo），5 次重启零回归。

---

## 1. 元器件状态总表（2026-07-26 实测）

| 子系统 | 状态 | 备注 |
|---|---|---|
| 运动板 + 12 电机 | ✅ | LCM 500Hz；eth0 ~5075 pkt/s |
| 机身 IMU | ✅ | LCM myIMU ~1000Hz |
| Wi-Fi (8821cu) | ✅ | 冷启自动上线 |
| 麦克风阵列 (RT5680 6ch) | ✅ | 出厂 `athena_audio` 节点持续采集中 |
| 扬声器 (TAS5805M) | ✅ | **机主亲耳验证出厂语音播报**；功放 `0x71=0x00` 无故障 |
| 双风扇 / 散热 | ✅ | 6/6 温区 enabled + 用户态 `fanboy` 双保险 |
| CPU 功耗模式 | ✅ | pmode 2 = `MODE_15W_6CORE`，6 核 |
| D455 深度相机 | ✅ | 848x480@30 三路 + IMU 200Hz，`d455-camera.service` 冷启自启 |
| **背部触摸板** | ✅ | `event0=synaptics_dsx`，出厂 `touch_publisher` 已 activate ⏸️物理触摸未由人验证 |
| **ramoops 黑匣子** | ✅ | 完整开机日志自动落盘归档（见 §3） |
| 蓝牙 GATT | ✅ | `jp5-bluetooth-gatt.service` 自启 |
| 系统时钟 | ✅ | `cyberdog-timekeeper` 单调地板 |
| zram | ✅ | 6 个设备 |
| CAN 总线 | ✅ | `can0 UP / ERROR-ACTIVE` |
| GPS (BCM4775) | ⚠️ **半通** | 硬件层通、驱动点火、`/dev/ttyTHS0` 属主已修；**出厂节点没有串口读线程 → ROS 数据链没通** |
| 超声×4 / ToF / 光线 / LED | ⚠️ **未端到端验证** | CAN 活；静默是出厂设计（`enable_count=0`，需上层发 ENABLE service） |
| **AI 头顶相机（3 sensor）** | ❌ | RCE 活(cmd=5)、IVC 通、3 颗 subdev 全 bound —— **卡在采集控制面 ABI**（见 §4） |
| 硬件看门狗 | ❌ 有意不做 | 末级到期是 system POR，会把 ramoops 一起抹掉；出厂 JP4 同样禁用 |
| 电池 / BMS | ⏸️ 物理 | **已确诊报废并被机主物理拆除**，当前纯适配器供电，机主稍后自购新电池 |

---

## 2. 本轮（0726）新完成的两件事

### 2.1 触摸板打通 + 炸机元凶定位
- 驱动 4.9→5.10 移植真实只有 **3 文件 56 行**（审计估"数千行"是高估）。
  方法论：**别照经验清单猜雷区，把 4.9 源码原样丢进 5.10 树让编译器报账。**
- DT 纠正两个错值：`irq-gpio` 边沿 `0x2002` → **电平低 `0x2008`**
  （RMI4 中断在状态寄存器被读走前一直拉低，边沿触发丢一次沿就永久卡死）；
  补回 `cap-button-codes`（缺它 `-ENODEV` 打掉整个 probe）。
- **炸机元凶用 A/B 对照坐实**：Image 里 `camrtc_device_group` 标记 炸掉的=2 / 撤除后=0 → 80 秒正常启动。
  其余三项（触摸板、H2 拆分、R32 采集 ABI）全部保留且工作正常 → **精确锁定在 NVCSI 电源域那一项**。

### 2.2 ramoops 黑匣子（零内核零 DTB）
只在 eMMC p1 `/boot/extlinux/extlinux.conf` 的 `LABEL jp5` 的 `APPEND` 加一串参数。详见 `PORT-STATUS.md`。
**三个非直觉的坑**（都实测踩过）：
1. 🔴 **`systemd-pstore` 的 `Unlink=yes` 会 zap 掉当前这次开机的 console 环形区** ——
   `ramoops_pstore_erase(PSTORE_TYPE_CONSOLE)` → `persistent_ram_zap(cprz)`。它在 ~17s 跑，
   等于每次开机抹掉 `[0..17s]`，**而那正是驱动 probe 挂死的窗口，黑匣子最该看见的地方**。
   → `/etc/systemd/pstore.conf` 设 `Unlink=no`。
2. **carveout 地址要用差值法定**：`引导器 /memory 节点` 减 `内核 memblock.memory`。
   ⚠️ iomem 里另有个同样 2MiB 的洞是**引导器的**，用了会静默写坏（`request_mem_region` 拦不住）。
3. `/dev/kmsg` 无前缀写**进不了**环形区（`default_message_loglevel=6` 被过滤），留面包屑要带 `<4>`。

**⚠️ 覆盖边界，别高估**：panic/oops（`panic=15` 热重启）和异常重启能拿到；
**真挂死需要拔电的，DRAM 掉电即失**。热跳闸断电同理。

---

## 3. 黑匣子怎么用（下次出事第一件事）

```bash
ssh mi@10.0.0.219 'ls -1 /var/log/cyberdog-blackbox/'
```
- 目录名带 **`-CRASH`** = 上次是真崩溃（有 `dmesg-ramoops-*`），一眼可辨
- 每个目录里 `console-ramoops-0` = **上一次开机的完整 console 日志**，从 `[0.000000]` 到最后一行
- 保留最近 30 次

---

## 4. AI 头顶相机：现在到底卡在哪

已推倒的墙（全部实测通过）：RCE legacy hsp 邮箱回移 ✅ / `diag@5` CH_SETUP ✅ /
nvmap WRITE ABI ✅ / **nvmap handle-as-fd 核心手术 ✅** / 采集 ioctl 参数墙 ✅

**剩下的是结构性的 ABI 分家，三条独立硬证据**（详见 `PORT-STATUS.md`）：
1. `CAPTURE_CHANNEL_SETUP_REQ` 消息号 R32=`0x10` / R35=`0x1E`，**R32 固件根本不认识 R35 发的**
2. `capture_channel_config` 216B→272B 全错位；ISP 侧两边**都是 0x20** → **静默乱掉而非报错**
3. 每帧内存模型换范式（R32 reloc vs R35 buffer-table），R32 **没有** `VI_CAPTURE_BUFFER_REQUEST` 这个 ioctl 号

**另有一条 0726 查明的根因**：R35 把"谁给 VI/ISP/NVCSI 上电"整体搬进了 RCE 固件，
而狗跑的是**不可刷写的 R32 固件**（它假定内核已上好电）。补回电源域的代码已落树并编译过，
**但正是它把内核炸了**（probe 阶段挂死，守卫够不着）。

> **下次重做 NVCSI 电源域前必须先做的事**：把 `Image.failed-kernelE` 拿来分析，
> 搞清它到底在 probe 哪一步挂死。**现在有黑匣子了，重试的代价比上次低得多** ——
> 但注意黑匣子只在"能热重启"时有效，probe 挂死通常要拔电，所以**串口日志（§6.5）才是这条线的关键前置**。

---

## 5. 🩸 血的教训（这些是花了真代价换来的）

1. **一次只引入一个可能影响启动的变量。**
   kernel-E 把 NVCSI 电源域（动硬件初始化路径）+ 触摸板 + DTB 拆分 + ramoops 打包进一次重启，
   炸了之后无法定位，走了一整轮 RCM 救砖。
2. **initrd 里的自动回滚守卫保护不了"内核在到达 initrd 之前就死"** —— 守卫自己活在 initrd 里。
   **动 power-domains / resets / clocks 时这个窗口是裸奔的。**
3. **动手前先验证救援链路可用**，别在没验证过的救援路径上押注。
   （`--rcm-boot` 七轮全败，`--boot recovery` 裸机刷写一次成功。见 `cyberdog-emmc-rescue-method-0726` 记忆条目）
4. **别猜，去测。**
   - 移植雷区：别照经验清单猜，**把老源码丢进新树让编译器报账**（触摸板从"估数千行"变成实际 56 行）
   - 内存地址：别看空洞猜，用**差值法**（引导器 `/memory` 减内核 `memblock`）
5. **`pristine` 整文件还原是陷阱** —— 快照时间点可能早于后来的改动。
   nvcsi 的 pristine `.dts` 里没有 H2 拆分那行 include，整文件还原会连 H2 一起悄悄撤掉。
   **要外科式只删该删的那一行。**
6. **成功/失败信号必须无歧义。**
   - 我把"狗自己关机"设计成成功信号 → 机主正确指出：电池报废时这和掉电**无法区分**。改成风扇脉冲模式。
   - 我把"上电风扇猛转 1 秒"当成自制 initramfs 的开工信号 → 那是**每次上电都有的自然现象**，白走两轮。
7. **`PartOf=` 只传播 stop 不传播 start** → 重启栈时 nginx / 蓝牙被静默杀掉且再没起来。
   要自动跟随必须用 `WantedBy=`。
8. **共用的 `ExecStartPre` 是单点** —— `jp5-chroot-prep.sh` 被 stack/nginx/bluetooth-gatt 三者共用，
   里面任何一条非零退出会让三个服务一起起不来。可选挂载必须有兜底，核心挂载才该响亮失败。
9. **只有真 `systemctl start` 才暴露环境问题** —— 蓝牙 GATT 的 `HOME` 缺失（`rcutils_expand_user failed`）
   在交互式试跑里永远看不见（那时 HOME 是有的）。
10. **改配置前先查该版本支不支持** —— `module-udev-detect profile_set=` 在 PulseAudio 11.1 上不支持，
    直接让整个 pulse 起不来、音频全哑。

---

## 6. 后面的计划（建议顺序）

### 6.1 🟢 低风险 · 高价值 · 可立刻做
**`ov_msckf` 单目 VIO**（六路综合评为"最有性价比的下一步"）
- 纯感知、**不碰电机**、不改内核
- 三个输入话题实测都在发，`ldd` 零缺库，内参已硬编码在出厂 launch 里
- 只需要一个 chroot 包装 unit + 一个 lifecycle activate

### 6.2 🟢 低风险 · 补验证缺口
**超声×4 / ToF / 光线 / LED 端到端验证**
- CAN 已证实是活的，静默是出厂设计（需上层发 ENABLE service）
- 属于"最后一公里没走"，不是"不通"

### 6.3 🟡 中等 · 独立工程
**GPS ROS 数据链**
- 硬件层实测通（`[SSPBBD]` 同 JP4），二进制打补丁已落地
- 真问题：**出厂生产节点没有串口读线程**，且复位只在节点构造期发生，`GPS_START` 不触发
- 要么自己写个读串口→发话题的桥，要么继续改二进制

### 6.4 🔴 高风险 · 需要前置条件
**NVCSI 电源域重做（AI 头顶相机）**
- **前置**：先做 6.5（串口日志），否则 probe 挂死还是拿不到现场
- 拿 `Image.failed-kernelE` 先做静态分析，别直接重试
- 严格遵守"一次只改一个变量"

### 6.5 🟢 补黑匣子的覆盖缺口
**救援笔记本上常驻串口日志**（`ben@10.0.0.176` 的 `/dev/ttyACM0` → 落盘）
- 这是**唯一**能拿到"挂死需拔电"场景的手段，与 ramoops 互补
- ⚠️ 本轮笔记本 key 认证不通（`Permission denied (publickey,password)`），**需要机主帮忙恢复访问**

### 6.6 ⏸️ 需要机主在场 / 需要新电池
| 事项 | 阻塞原因 |
|---|---|
| 站立复测（pmode 2 = 1.42GHz，此前是 1.9GHz 验证的） | 会动电机，**必须机主在场** |
| 满载 30 分钟热测试 | 需要稳定供电 |
| 真·冷启动验收 | 需要电池 |
| 触摸板物理触摸确认 | 需要人摸一下 |
| LED 目视确认具体颜色 | 需要人看 |

---

## 7. ⛔ 安全红线（务必继承）

- **任何可能动电机的指令必须机主在场。** `move_base_node` 会直接发 `SE3VelocityCMD` 到 `body_cmd`/`cmd_vel`
  → **机主不在场绝不启动 `automation_launch.py`**
- **别盲目驱动 MCU 使能脚**（会连带敲掉 Wi-Fi）
- **别对 active 的 `camera_server` 调 `configure`**
- **别远程 `systemctl isolate multi-user.target`**（会同时断掉 SSH 和 fanboy）
- **别"修" PWM 极性** —— 极性反转是硬件事实，DTB 降序 `cooling-levels` 已补偿，**改了才会散热失效**
- **别绕过 pulseaudio 走 ALSA 直通** —— 16k 会被 BE 抽干 → 功放 CLK 故障锁存（要断电才清）
- **`ota_server` 绝不能上** —— 混血系统上它按 JP4 分区认知去刷，一次 OTA 能把三重安全网连同引导一起写废
- **别用 `echo c > /proc/sysrq-trigger` 测 ramoops** —— `panic=15` + USB 插着有进 RCM 的风险
- **别在笔记本上跑 `apt-get autoremove --purge`** —— 会 purge python2，出厂 `BUP_generator.py` 依赖它

---

## 8. 常用命令

```bash
# 连狗
ssh mi@10.0.0.219

# 黑匣子
ssh mi@10.0.0.219 'ls -1 /var/log/cyberdog-blackbox/'

# 一把梭健康检查
ssh mi@10.0.0.219 'sudo bash -s' <<'EOF'
echo "温区 : $(cat /sys/class/thermal/thermal_zone*/mode|grep -c enabled)/6"
echo "核数 : $(nproc)  pmode: $(nvpmodel -q|tail -1)"
echo "failed: $(systemctl --failed --no-legend|wc -l)"
echo "video : $(ls /dev/video*|wc -l)   声卡: $(grep -c '^ *[0-9] \[' /proc/asound/cards)"
echo "rmem  : $(sysctl -n net.core.rmem_max)"
echo "崩溃  : $(dmesg|grep -ciE 'Unable to handle|Internal error|BUG:|Call trace')"
EOF

# 挂 eMMC p1（真引导配置所在）
ssh mi@10.0.0.219 'sudo mkdir -p /mnt/emmcp1 && sudo mount /dev/mmcblk0p1 /mnt/emmcp1'
```

---

## 9. 关键文件位置

| 位置 | 内容 |
|---|---|
| `PORT-STATUS.md` | **权威现状**：部署铁律、⛔禁做清单、被推翻的旧结论、各专题详解 |
| `LESSONS-CYBERDOG-2026-07-23.md` | 早期经验教训总纲 |
| eMMC `p1:/boot/extlinux/extlinux.conf` | **真引导配置**（nvme 上那两份是幌子） |
| eMMC `p1:/boot-jp5/` | JP5 内核 + DTB + initrd，含 8 代备份 + `Image.failed-kernelE` |
| `tools/phase4/initrd-rootfs/sbin/jp5-autorevert-hook` | **权威版**自动回滚守卫（`build/artifacts/` 那份是旧的，会静默降级） |
| `build/ramoops/` | 本轮产物 |
| `build/touchpad-batch/out/` | 当前部署的内核/DTB |
