# JP5 站立之路：打通记录（2026-07-22/23）

> ## 🏆 2026-07-23 JP5 首站成功
> 充电修复（DC 口接触）→ 充到静置 20.7V → **机主长按电源真断电仪式**（清板端锁存的正解——`shutdown` 只关 Jetson，电源板/灯还活着！）→ 开机全自启全绿（Locking=0）→ `stand.sh` 一发成功：`Auto recovery_stand succeed`，gait 3，站立电压 19.78V（跌幅 0.5V）。
> **两条新操作铁律**：① 真关机=长按电源到灯全灭（远程 shutdown 不断电源板）；② 适配器插入疑似触发运动锁定（风扇停=运动域休眠）——**站立前必须拔适配器，站立中严禁插**。

**（以下为过程记录）JP5 下的完整运动能力已就绪并自愈化。**

## 架构（现行方案）

JP5（p2，5.10.216-tegra）为 host，**chroot 运行 p1 的 JP4 原厂 Foxy 栈**：
- `jp5-cyberdog-net.service` → `/usr/local/bin/jp5-cyberdog-net.sh`：自力保证 l4tbr0 up + 192.168.55.1/24（不依赖 NVIDIA gadget 脚本，它在无线冷启动下会摆烂）、eth0 并桥、224.0.0.0/4 组播路由、can0 1Mbps、eth0 脱离 NM 管理。
- `jp5-cyberdog-stack.service`：ExecStartPre = `jp5-chroot-prep.sh`（挂 p1 + bind dev/pts/shm/proc/sys）+ `jp5-lcm-wait.sh`（**等到 state_estimator 真包到达才放行**，杀启动竞态）；ExecStart = chroot su mi `/home/mi/jp5-stack-inner.sh`（env + ros2 launch athena_bringup lc_bringup_launch.py）。
- `jp5-stack-doctor.timer`（2min）：栈聋（Motion offline 刷屏）但线上有 LCM 流 → 自动重启栈。**已实战验证有效。**
- 一键脚本（JP5 host ~/）：`stand.sh` / `liedown.sh` / `dogstatus.sh` → chroot 内 `*-inner.sh`。
- `jp5-net-watchdog.timer` 保持 OnBootSec=30min（单元描述里的"6 min"是陈旧文案，别被骗）。

## 打通过程中修的四个真问题

1. **chroot 里 DDS 全断**：p1 的 `/etc/hosts` 根本没有 `localhost` 行（原生启动靠 systemd nss 模块掩盖）→ CycloneDDS `add_peer_addresses: localhost: unknown address` → 进程间发现全灭、lifecycle 级联不激活。修：hosts 补行 + cyclonedds.xml peers `localhost→127.0.0.1`（备份 `.bak-jp5chroot`）。**这大概率也是当年 rs_bridge DDS 投递之谜的亲戚。**
2. **无线冷启动下 l4tbr0 半死**（DOWN 无 IP）→ LCM 加组 ENODEV。修：net.sh 自己拉桥配 IP。
3. **栈启动早于板流就绪** → LCM 加组竞态失效。修：lcm-wait 门 + doctor 自愈。
4. **eth0**：JP5 的 nvethernet(EQOS 2490000) 原生好使，1Gbps link up，运动板 192.168.55.233 直通——**站立完全不需要碰任何 MCU/expander GPIO**（VBUS_MOTION_EN 在 JP4 工作态就是未驱动的输入；RSTN_HUB 同样别碰）。

## 🎯 充电之谜终局（2026-07-23 晨，实测破案）

**凶手：DC 插头/充电口接触不良（边缘接触）。电池完全无辜。**
- BMS 原始帧（LCM bms_data@7672 解码）：电池 #2 **health=100%、循环数=4（近乎全新）**、无任何故障位、温度 35°C 正常。
- 机主按指令重插并稳住插头后：**充电立即全速运行**——电流 0.4~2.9A 脉冲灌入，电压 3 分钟 18.57→19.60V。接触边缘态解释此前一切：好的时候充电、半接触时只能检测+2秒中止脉冲、断开时"适配器消失"。
- 昨晚"过夜零充电"：适配器整晚物理插着、电压纹丝不动。两种解释待分（实用上无所谓）：①高阻接触——空载能被检测、一上充电电流接触点压降大→2秒中止（完美解释昨晚 status 1↔5 脉冲）；②本机关机时不执行充电协商。**实用规则：充电=开机 + 插头按到位 + 确认充电位亮/电流为正。** 晨间（开机、拔过再用力插回）后立即满速充电。可选后续实验：充满后做 5 分钟关机充电对照，钉死②。
- **修复建议**：清洁 DC 母座、检查插针是否外扩；临时用胶带固定插头姿势；长期考虑换母座或插头。电池 #1（17%那块）大概率同样健康，接触良好时插狗上即可充回。
- 站立门槛：电压 ≥21V 后站立（上午 JP4 站立时 22.7V 参考）。监控脚本 /tmp/bmssniff.py（解码字段全）。

## 电池/充电旧推演过程（截至前晚，部分结论已被上文推翻）

- 上午旧电池(#1)：16.7V/1%，板端 12 电机全报欠压锁定 → 换电池(#2, 52%/22.7V) + **全断电冷启动清锁存** → **JP4 一条命令站立成功**（14:26，跌幅仅 0.26V）。
- 下午切 JP5 后两次起立超时 + 16:03:42 整机硬死 + 5 连环重启：**不是软件**——#2 电池在 15:5x 冷启动后从 47%/22.98V 崩到 1%/17.38V（老化电池的容量悬崖 + 开机浪涌反复压死系统）。**起立指令确认送达运动板并被执行（TX 链路好的）**。
- 机主关键观察：#1 装上时约 17%，插着适配器一路掉到 1% ——**插电只耗不充**。两块电池同症状。
- 实测：适配器插入时 BMS 每分钟做 2 秒充电尝试即中止（status bit2 脉冲）；电压 18.2V 平台上以 ~5mV/min 极缓爬升。**当前判断：充电路径疑似活着但卡在深放电预充涓流（<3.3V/芯只给小电流），开机负载 >> 涓流 → 永远净耗电。**
- **进行中的实验：适配器插着、整机关机过夜** → 涓流不再被吃掉。明早电压 >20V = 判定成立且问题自愈；纹丝不动 = 充电路径真坏（→ 万用表量适配器输出，按适配器铭牌电压对照）。

## BMS 协议解码（晚间新增，明日取证用）

- **status 位定义**（开源 bms_common.hpp）：bit0=USB/适配器插入、bit1=低电量、bit2=充电中、bit3=充满、bit4=短路、bit5=过温、bit6=BMS错误。实测：适配器识别正常（bit0 恒亮）、充电位每分钟脉冲亮 2 秒即灭、**从未出现任何故障位**。
- **数据通道**：LCM `bms_data`@239.255.76.67:7672（每 ~2.5-5s 一帧）；命令通道 `bms_command`（bms_request_lcmt：charge_enable / power_supply[bit2=整机断电，勿碰!] / buzz）。
- **bms_response_lcmt 字段**（大端，LCM 8字节hash后）：int16 volt(mV)、**int16 curr(mA)**、int16 temp、int8 soc、int8 status、int8 key、**int8 health(%)**、**int16 loop_number(循环数)**、int8 **powerBoard_status(bit0=电源板↔电池串口通信错误)**。
- **嫌疑焦点**：今早日志出现过 `bms_led_control report error`（给电池包发命令失败）→ 电源板与电池包的**串口触点接触不良**可解释一切（充电协商 2 秒失败、两块电池同症状、外置智能充电器也失败）。明日开机：python 监听 7672 解码 batt_curr（充电脉冲期间有无真实电流）+ powerBoard_status bit0 → 三叉裁决：串口触点 / 充电电路 / 电池保护锁。

## 明日站立 runbook（若晨检电压 ≥20V）

1. 开机（什么都不用做，全自启）。
2. `ssh mi@10.0.0.219 '~/dogstatus.sh'` 看电压 + Locking 是否为 0。
3. 机主在场：`ssh mi@10.0.0.219 '~/stand.sh'` → JP5 首站。
4. 站立成功后趴下 `~/liedown.sh`，然后继续 Phase 5（相机原生 V4L2 优先试）。

## 已知误判存档（防后续会话踩坑）

- ~~"下午有人换回了旧电池"~~ ——机主确认没换过；是 #2 自己崩到悬崖下。
- ~~"5 连环重启是 net-watchdog 干的"~~ ——timer 实际 30min，是电池崩溃循环。
- "Odom offline"日志与站立无关的结论见 STAND-TRUTH-2026-07-22.md（依然成立）。
