# 交接快照 2026-07-29 —— AI 相机战役收官

**权威状态文档 = [PORT-STATUS.md](PORT-STATUS.md)（含全部证据链）。本文只是索引。**

## 今天发生了什么（按时间）

1. **零帧墙倒**：绕开 argus 走 v4l2 直接出真图 ⇒ 内核/DTB/RCE 链全通，
   「零帧」只属于 argus 路径（R32 闭源 libargus ↔ R35 capture chardev 的 ABI）。
2. **v4l2→ROS2 桥**：Python 版 → 30fps 攻坚（四步：array.array / 墙钟节流 /
   LUT / 节流容差）→ C++ 版直出出厂规格 **1280×960 rgb8 @30fps**。
3. **AC 转正**（机主拍板）：DEFAULT jp5 引导带相机补丁的内核。
4. **全镜头攻坚**：查明「rebind 后只有第一路能出帧」= 自家补丁重跑
   start_streams 自伤（对在流 lane 重校准）。stage24(once)+stage25(端口位图)
   → **变体 AE 转正**，三路并发全满速。
5. **三镜头服务化**：主摄 + `fisheye_left`(2-0061) + `fisheye_right`(2-0062)
   开机自启，产线个体标定（/params/camera）已接入 camera_info。

## 当前状态一句话

`DEFAULT jp5` → AE(#44, `d2abc750a65f85ed`)，开机全自动三话题
（29.6/29.7/29.7 fps），0 failed / 8 服务 / 38 ROS 节点。
回退链：`Image.prev-ae`(=AC) → `Image.prev-ac`(=good #9)。

## 明面上的余量（都不阻塞日常使用）

- **出厂 camera_server 的拍照/录像/APP 图传仍废**（argus 路径）。修法两条：
  啃 nvmap handle ABI（大工程）或用桥转 RTMP 喂出厂 nginx（中工程）。
- **出厂 AI 跟踪**缺 bbox 生产者（出厂绑死 argus 内嵌 SDK）。可自建检测节点
  订阅我们的话题发 bbox（新工程，见 PORT-STATUS 调查结论）。
- **双目 VIO 升级**：双鱼眼 + `extrinsics_stereo`(基线 79.9mm) 家底齐了，
  ov_msckf 可从 D455 单目升双鱼眼（新工程）。
- 鱼眼左右对应由 DTB badge 推断（`ov7251_l_center`=left），白天可用手遮镜头终验。
- ⚠️ 夜里图像噪声大是暗光正常现象，白天验图更直观。

## 安全红线（不变）

动电机必须机主在场；不碰 active camera_server 的 configure；ota_server 永不启用；
不"修"PWM 极性；不绕过 pulseaudio；拔电前先拔 USB（防 RCM）。
