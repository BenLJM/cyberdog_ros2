# CyberDog 一代逆向开源：可行性总判决与分级路线图

> 综合 7 个域盘点 + 2 份对抗质疑。**凡质疑与盘点冲突处，一律采用质疑的修正版本**；两份质疑互相冲突处（有一处），取更保守的一方并标出争议。
> 报告中所有事实分四级：**【已验证】**（本项目或质疑方实机/实文件测过）、**【推断】**（有证据链但未直接验证）、**【猜测】**（无证据）、**【未验证】**（明确没测过）。
> 本次撰写期间我另做了 6 项复核（见文末附录），其中 CPU 空闲率一项修正了盘点数据。

---

## 1. 一句话判决

**CyberDog 一代能被逆向开源到「完整二次开发平台」级，但不能被还原成「出厂整机」——分三层：硬件抽象与控制接口层可做到 90% 以上（完全）；出厂上层能力（视觉 AI／导航自主／语音助手）只能做到「替代重写」而非「还原」，属大部分；维修级硬件工作（原理图／CAD／执行器固件）只能做外围。**

再压一句话给机主：**技术上没有过不去的墙，但当前有三个非技术前置卡着——这台狗没有电池（所有运动验证物理中断）、仓库连 LICENSE 都没有（法律上不能发）、四份不可再生资产零备份（随时可能永久损失）。** 在这三件事解决前，讨论「补全七个域要几人月」是虚的。

**判决的边界要说清楚**：
- 「完整二次开发」= 开发者能读懂这台机器、安全地控制它、写自己的 ROS2 节点、跑自己的步态/视觉/导航算法。这个目标**成立**。
- 「还原出厂体验」= 手机 App 一按就跟着人走、喊一声就应答、自主建图导航。这个目标**不成立**，只能用开源件重建功能等价物，行为不会一样。
- 「修好一台坏了的狗」= 换外壳、换电机、诊断电源板。这个目标**基本不成立**（无原理图、无 CAD、机主不能拆机、执行器固件闭源）。

---

## 2. 完成度现状表

两列必须分开看：**「域的开放度」**（客观上小米留了多少门）与 **「我们的进度」**（这台狗上已经做到哪）。盘点里最容易混淆的就是这两者——运动域开放度很高，但我们的进度接近零。

| 域 | 判档 | 域的开放度 | 我们已有 | 剩余卡点性质 | 剩余量级（单人） |
|---|---|---|---|---|---|
| **BSP／内核／引导** | **可补全**（最接近可交付） | 高（GPLv2 义务下出厂内核全树已公开 + zbwu 刷机 SDK 公开） | **~70%**：JP5 内核 5.10.216 跑起来、三路相机满速、触摸板/GPS/音频/ramoops/热区全通、RCM 救砖实战两次、32MB QSPI 全片备份 | 工程卫生：相机 17 个 stage 补丁**只以 Python 注入器存在**，仓库无源码级快照【已验证：`kernel-tree-mods` 最后提交停在 07-26，grep `r32_camera_power` 零命中】 | **3 人月起**（整理 2.5 + 至少一轮 pristine 树重放与实机复现） |
| **运动控制与底盘** | **可补全**（路径清晰，零实机验证） | **最高**：控制器本体 Apache-2.0 开源在 `cyberdog_locomotion`，官方给了免签名开发通道 `motor_sdk`，28 条 LCM 定义全开源 | **~10%**：只做过被动抓包（只读，未发任何指令），协议已读通 | ①官方 README 原文：**「只在铁蛋2代上进行了充分测试。对于铁蛋1代…未经充分测试，请谨慎使用」**，且部署脚本会 `kill -9` 工厂控制器再顶上去②**电池已拆除**，物理上无法验证 | **1~2 人月**（含 1 代未测功能逐项调通），**且被电池整体阻塞** |
| **视觉 AI** | 中高（头文件全在狗上、二代 `cyberdog_vision` 开源可参考、人脸模型 onnx 齐全） | **~35%**：图像源已解决（v4l2→ROS2 C++ 桥，主摄 1280×960@30fps + 双鱼眼），这是最贵的一段 | **难补全→可补全，取决于一条命令**：TensorRT 能否在 chroot 跑，**从未执行过一次推理** | body 检测三个模型**只有 TensorRT 7 序列化 plan，无 onnx/caffe 源**（不可还原，只能替代）；深度源冲突（见 §3.6） | **2.5~3 人月**（主链单独 1.5~2 人月），**前置 trtexec 验证** |
| **语音与交互** | 中（ROS 胶水 100% 开源，5 个闭源 .so 今天仍可从小米 CDN 公开下载） | **~40%**：麦+喇叭硬件链路已打通，本地 ASR/NLP 加载成功 | **保留小米二进制 = 可补全；彻底去小米 = 难补全** | ①**唤醒词从未被验证触发过一次**（`check keyword` 计数 = 0）②当前 card 1 没进 pulseaudio，**出厂语音播报是静音的（已确认回归）**③`libvpm`（6 麦 AEC/DOA/KWS）完整替代 = 数人月+ | 用小米件：**人周**；去小米：**数人月+** |
| **导航与 SLAM** | 高（算法层几乎 100% 是上游开源；仅 6 个闭源包，其中 2 个是死代码/上游改名） | **~20%**：单目 VIO 在跑、D455 在跑 | **可补全**，但不是"写 300 行胶水" | 要在 Foxy 上把 nav2 全栈编起来 + 给 costmap 找传感器层（无激光雷达、深度已退化）+ 定位方案；**从未跑通过一次端到端导航** | **人月级起**，目标应定为「与 move_base2 能力对等」而非「更强」 |
| **App／云／连接／OTA** | **最高**：gRPC 明文无鉴权、proto 开源、蓝牙 GATT 协议全开源、社区已有可跑的 Python 控制端 | **~50%**：协议已读通、蓝牙 GATT 在 JP5 跑起来了 | **已解决（控制通道）／不可行（小爱云）** | 小米云不可自建；MCU 固件包**加密**不可重建 | 开源控制端 CLI：**人日~人周** |
| **已开源部分的可构建性** | — | **~30%**：知道该改哪四处，但 fork 没做 | **可补全** | ①Foxy／Galactic 双轨错配（仓库自相矛盾）②官方构建链腐化（fastgit 已死）③**`cyberdog_ros2/` 在本仓被 .gitignore 整个排除，零入库**【已验证】 | **人周~人月** |
| **⚠️ 电源与电池（无人认领）** | — | **0%** | **难补全（阻塞其余四域）** | 【已验证 13:16】`/sys/class/power_supply/` 只有 `usb-charger`，**无 battery 节点**；插适配器触发出厂运动锁存 → 插着不能动、拔了直接掉电 | 采购 + 兼容性验证 **人日级，但有交付周期** |
| **⚠️ 法律／许可（无人认领）** | — | **0%** | **可补全（但是发布的先决条件）** | 【已验证】仓库根 `LICENSE*/COPYING*/NOTICE*` 零匹配；内核补丁（GPL-2.0 义务）与自研用户态（可宽松）混在一个仓 | **人日~人周** |
| **⚠️ 可复现性（无人认领）** | — | **0%** | **难补全（需外部条件）** | 样本量 = 1，且是**二手机**（前任遗留 LXDE 自启项已被记录），基线已被人动过 | 需第二台狗 |

**总账**：七域全做完（单人）**约 10~18 人月**，且**不含**跨域集成、文档英译、第二台设备验证、安全与法律工作、以及发布之后的维护。盘点里没有任何一个域做过这个加总，五个域的 verdict 工作量小于它自己 items 的加总。

---

## 3. 硬阻碍清单

### 3.1 真正过不去的墙（五堵）

**① Dreame 加密的运动板固件——【已验证，且这是一处事实纠正】**
出厂 `p2151_update-*.img` 的 PKCS#7 OID 实测是 `1.2.840.113549.1.7.**3**` = **envelopedData（加密）**，不是 `.7.2` signedData（签名）。盘点原文说"签名"是错的。
后果比"签名"严重得多：**镜像内容读不出来**——不能反编译、不能 diff、不能做「工厂控制器 vs 开源控制器」的行为对照。
→ 连带推翻运动域的核心判断：「板上跑的就是这套开源码」**在二进制层面零证据**，只有 LCM 接口同源这一间接证据【已验证：`motion_control_request_lcmt` 结构算出正好 122 字节 + 8 字节 fingerprint = 抓包的 130B，接口同源成立】。正确表述应是：**「板子跑的控制器与这套开源码共享同一套 LCM 接口契约」**，而"改步态"实际是**整体替换**一份官方自认 1 代未充分测试的控制器，不是"修改"。

**② 小米云（小爱 / AIVS）——不可行**
端点全在 `*.ai.xiaomi.com`，服务端仍活着但这台狗的 token 已过期、refresh 失败（401 `OAuth token is illegal or expired`）。这是他人的云服务，不存在"补全"一说。
→ 绕过后的能力边界：**对话式问答与云 TTS 永久失去**；本地唤醒→本地 ASR→本地 NLP→8 条预制动作这条链路不依赖云，可以活。

**③ 执行器（12 电机）CAN 固件 / MCU 固件**
在 `motor_sdk` 抽象层之下，闭源无源码，且被 ① 的加密保护。
→ 边界：**能完整控制电机（位置/速度/力矩/kp/kd），不能改电机行为、不能换电机。** 对二次开发够用，对维修不够用。

**④ 硬件电气文档（原理图/BOM/接线表/CAD/URDF）**
小米从未公开；机主明确不能拆机 → **本项目条件下无法测绘**。官方 issue #174（外壳摔碎无法 3D 打印）、#181（抱怨结构与控制配置至今保留）全部 Open 至今。
→ 边界：能从 bootloader-dtb 反推出**功能级**拓扑（I2C 器件全图、电源域、相机连线，已做到且质量很高），但拿不到**电气级**（网表、每路稳压器最大电流、连接器针脚）。照着 DTB 接新硬件而不量电压电流有烧板风险。

**⑤ 出厂人体检测模型不可还原**
`detect-batchsize1.engine` / `cls_human_mid.engine` / `reid_v1_mid.engine` **只有 TensorRT 7 序列化 plan，没有任何 onnx/caffe 源**【已验证：body 目录全量 ls 只有 3 个 .engine；对照 face 目录 onnx/caffemodel/prototxt 全齐】。
→ 边界：**不能还原，只能替代。** 好消息是代价很小——从层名判定它是 2016 年代 Faster R-CNN + Inception 主干，YOLO11n 在精度和速度上都会赢。**正确目标是「替代」不是「还原」。**

### 3.2 半堵墙：libargus / 硬件 ISP（只在 JP5 路线上成立）

绕过 argus 之后画质链完全落在 CPU 上——**没有 AE 闭环、没有 AWB、没有降噪、没有 HDR**。
→ 这堵墙**在 JP4 基线上不存在**（argus 通、ISP 可用）。这是 §4 里"基线路线之争"必须先拍板的直接原因。

### 3.3 非技术的硬前置（比技术墙更近、更现实）

| 前置 | 状态 | 影响面 |
|---|---|---|
| **电池** | 【已验证】已物理拆除并确诊报废；插适配器 = 出厂运动锁存 | **运动域 4 条「已解决」、导航域全部上机项、视觉域跟随、语音域「首次唤醒→动作」，今天一次也执行不了** |
| **LICENSE** | 【已验证】仓库根零匹配 | 不能公开发布 |
| **四份不可再生资产零备份** | 【已验证】`/params`(1.7M) 仓库零副本；`build/qspi-backup-factory.bin`(32MB)、`build/mirror/`(7 棵树含 `athena_l4t_sdk.git`)、`cyberdog_ros2/` 全部被 .gitignore 排除、`git ls-files` 零命中 | 一次误操作或一次硬盘故障 = 永久损失 |
| **无串口 = 无安全网** | 【已验证改判】笔记本上那个 `/dev/ttyACM0` 是 USB gadget ttyGS0（T+19.4s 才存在），够不着 extlinux 菜单。三重安全网**实为两重** | 第三方刷机没有兜底 |
| **样本量 = 1，且是二手机** | 【已验证】序列号 1421621045904；前任遗留 LXDE 自启项已被记录 | 所有「出厂行为 = X」的断言带未量化偏差 |
| **零 CI、bus factor = 1** | 【已验证】无 `.github/workflows`；git shortlog 提交几乎全是同一人 | 已发生过至少三次静默回归（rmem_max 丢 123 倍、音频金标准固化调试期错误、热区复发） |

### 3.4 一处质疑之间的冲突（我取保守方）

- 质疑 A：实测 `/etc/athena-version` = `athena_2022.01.14_emmc_nvme_V1.0.0.94_release`，**正好卡在 motor_sdk 要求的 1.0.0.94 最低线**，可结案为"满足"。
- 质疑 B：Dreame 整包版本 `p2151_update-4.1.2_0071` 与 `robot-software` 版本是**两套无法互相比较的版本号体系**，登板也不是"比字符串"能回答的。

**我的裁定（保守）**：`/etc/athena-version` 是 **Jetson 侧 rootfs 的整机版本**，不等于运动板内部 `robot-software` 的版本。质疑 A 这一条属于混淆，应**维持为未验证**。已可结案的只有两条：`192.168.55.233` ping 通、TCP 22 OPEN。

### 3.5 一处需要拆开的互斥结论（OTA 攻击面）

App 域说「零签名 → 任何能连 10206 者可刷 MCU 后门」，运动域说「伪造签名不可行」。合起来应拆成三句：
1. **Jetson 侧 `athena_install`（dpkg 装任意 deb）是真实且严重的攻击面**，保留高危评级；
2. **MCU/运动板侧受镜像加密保护**，攻击者注入的内容运动板解不开 → 从"后门/炸机"清单降级；
3. 「零数字签名」这个断言只有静态符号缺失做支撑（`ota_server` 里同时存在 `CERT verify`/`Signature Algorithm`/`RSA Public Key` 字符串，虽更像静态链接的 openssl/curl，但符号缺失不能证明无校验逻辑）→ 应写作**「未发现对包体的签名校验（静态分析，未动态验证）」**。

### 3.6 一处被所有域漏掉的技术冲突：深度源

跟随需要 D455 深度；VIO 需要**关掉 D455 的 IR 点阵发射器**（开着会让散斑跟着相机走、产生无视差假特征，VIO 直接失效）。**二者互斥。**
三条出路，每一条都推高视觉主链的工作量：①跟随时停 VIO 并开发射器（但跟随下游要里程计，与停 VIO 打架）②接受室内弱纹理下被动双目深度的严重退化 ③改用双鱼眼自算深度（基线 -79.93mm 已标定，但狗上那版 OpenVINS 不认 MEI 模型，属新工程，人月级）。

---

## 4. 分级路线图

### 先拍一个板：**基线路线之争**（这个不定，下面所有工时估算都缺前提）

七个域默认交付物是「我们这台 JP5 内核 + chroot 跑 JP4 出厂栈」，但**没有一个域论证过这对第三方是不是最优基线**。反向证据恰恰散落在各域内部：视觉域正因为在 JP5 上，才要担心 TensorRT 能不能跑；BSP 域自己承认硬件 ISP 在 v4l2 路线下完全用不上。而 PORT-STATUS 自己就标着「JP5+chroot 是终局架构还是过渡态需要机主拍板」——**这个拍板从没做过。**

| 基线 | 第三方门槛 | 硬件能力（ISP/TRT） | 我们资产可迁移度 | 砖机风险 |
|---|---|---|---|---|
| **A. JP4 原生**（不碰内核） | **最低**（零刷机、无需 x86 主机） | **最全**（argus 通、ISP 可用、TRT7+出厂视觉 SDK 直接跑） | 中（协议/文档/控制端全可用；内核工作用不上） | **零** |
| B. JP5 + chroot（我们现在这条） | 高（需刷机 + x86 主机 + RCM 兜底） | 相机走 v4l2 裸流、**无 ISP**；TRT 能否跑**未验证** | 最高 | 中 |
| C. zbwu 全刷 R35 | 高 | 【未验证】其 README 称 color/stereo camera "work well"，若成立则 argus+ISP 直接可用 | 低（内核工作大部分作废） | **高**（换 MB2 等引导关键分区） |

**我的建议（与七域隐含假设相反）：面向社区首发应当是 A（JP4 基线的「缺件补全包」）**，因为它对第三方门槛最低、能力最全、砖机风险为零，且和所有人手里的出厂镜像一致。**JP5 BSP 作为进阶分支单独发布**——它是本项目最有技术含量的资产，但它是给硬核用户的，不是给"想二次开发的普通 ROS2 开发者"的。

---

### T0：立刻能发布（**2~3 人周**，零风险，不需要电池、不需要动电机、不需要刷机）

| 交付物 | 为什么值得做 | 风险 |
|---|---|---|
| **① 资产保全包**：`/params` 打包+sha256、`qspi-backup-factory.bin`、`build/mirror` 三棵关键树（`athena_l4t_sdk.git` 是刷机三件套的唯一来源）、`cyberdog_ros2` 锁 commit（或改 submodule）→ **异地备份** | **唯一一件"不做就可能永久损失"的事。** 四份都只存在于一台 Mac 的本地目录里 | 零。**这条应当今天就做** |
| **② `dogfingerprint.sh` + `dogcheck.sh`**（纯只读体检 + 回归断言：rmem_max / nvpmodel / thermal_zone / 声卡 profile 与 sink / 三路相机帧率 / 节点数与 failed 数 / TF 树） | 既是新用户的第一条命令，也是把「已解决」翻译成可执行断言的唯一办法。**没有断言的"已解决"，半年后等于没有**（本项目已被静默回归咬过至少三次） | 零 |
| **③ 事实勘误表** | 这是 T0 里对社区**信息价值最高**的一项：它把"闭源盲盒清单"直接砍掉一大半 —— `libvision_ai_sdk.so` **不存在**；`libpath_ops/libtf_help` 是上游 nav2 的 `nav_2d_utils`；`libdecisionmaker_core` 就是开源那四个 .cpp；`libminerva/libvpm/libpaddy` 是**语音**件不是视觉件；`libContentMotionAPI` 是视觉不是运动；`libdmp` 是导航不是运动；**手势/关键点/AutoTrack 一代根本没有实现**（消息定义存在但全 rootfs 零引用）；OTA 镜像是**加密**不是签名 | 零 |
| **④《硬件功能级拓扑手册》**（bootloader-dtb 反推：I2C 器件全图、fixed-regulators、相机连线、reserved-memory） | 纯离线分析，已能跑出来。**必须诚实标注：这是"软件视角的拓扑"，不等于电气规格** | 零 |
| **⑤ `RECOVERY.md`** + **《运动实验安全规程》** + **《安全基线》** | 恢复流程有四个实测坑（RCM 会话一次性、`--no-systemimg`、`NO_RECOVERY_IMG=1`、initramfs 必须 root 打包），且必须写明「串口救援是假的」。安全规程必须放在**任何驱动电机教程之前**——这是一台十几公斤、12 个力控关节、**没有硬件急停**的机器 | 零，但不做风险极高 |
| **⑥ 协议文档包**：28 条 LCM 定义 + gRPC proto（**用官方开源版**）+ 蓝牙 GATT + ROS2 话题/服务语义表 | 让开发者不用逆向就能接入 | 低。**从二进制恢复的那部分（`cyberdog_app_RECOVERED.proto`）单独标注来源与互操作目的**，不与开源 proto 混为一谈 |
| **⑦ 许可架构**：`build/**` = GPL-2.0-only（内核衍生，带 DCO/Signed-off-by）、`dog-scripts/`+`tools/` = Apache-2.0、文档 = CC-BY-4.0、每文件 SPDX 头、根 LICENSE + NOTICE | **发布的先决条件。** 给整仓统一挂 Apache-2.0 会直接与内核补丁的 GPL-2.0 冲突 | 零，但不做就不能发 |

**T0 的一条硬规则（写进 CI）**：发布物一律**零第三方二进制、零 rootfs 镜像、零 QSPI 内容、零 `/params` 真实标定、零 `token.toml`**。只发「URL + 大小 + SHA256 + 一键获取脚本」。异地镜像可以做，但只作私人备份，不入公开发布通道。

---

### T1：数人周内可达（**4~8 人周**，明确的、已知路线的补全）

| 交付物 | 为什么值得做 | 风险 |
|---|---|---|
| **① 采购电池并验通** | **解锁四个域的验证能力。** 第三方替换包确实在售（`P2151-6S2P-MMLS`，21.6V 6S2P 5000mAh/108Wh，与实测 6S 22.7V 吻合，P2151 正是运动板项目代号）。**不是"买不到"，而是"与出厂 BMS 的握手/SOC 上报/充电协商是否兼容 = 完全未验证"** | 中。BMS 在闭源运动板固件侧（`athena_bms` 符号含 `bms_response_lcmt`/`disable_charge`），DIY 电池包不是"配一块 6S 锂电"那么简单。108Wh 超航空 100Wh 限值 |
| **② 两个判决实验（各人日级，信息量最高）** | **(a)** chroot 内一条 `trtexec --loadEngine` → 同时回答"TRT 活不活"与"三个 .engine 的确切输入输出维度"，**决定整个视觉域生死**；**(b)** 修 AEC 参考通道 / card 1 pulseaudio 回归 → 然后对狗说一次「旺财旺财」grep 日志，**这个基本事实至今从未被验证过一次** | (a) 会占 GPU，建议机主在场；(b) 零写入零风险，但**链路一旦通，一句话直达电机，中间无二次确认** |
| **③ 基线三方对照表 + 拍板** | 不定这个，T2 全部工时估算缺前提 | 零 |
| **④ `cyberdog_ros2` fork 四处外科手术**：恢复 `cyberdog_vision`（`git show b35dc14^`，717 行完整在历史里）、删 `scenedetection` 那两行 `return()`、`mini.repos` 回锁 Foxy、`automation_launch.py` 的 9 个仓外包做成可选 | 让"今天 clone 下来能编出来" | 中。**Foxy 编的 .so 和 Galactic 编的 .so 进同一进程会直接段错误，且报错完全不指向根因**——这条必须写成红字 |
| **⑤ 开源 gRPC 控制端 CLI** | 社区已有可跑先例；50051 明文无鉴权、proto 开源 | 低。**自建控制端必须自加一层鉴权**，否则是在扩散攻击面 |
| **⑥ D455 双目 VIO 恢复**（`.stereobak` 就在盘上） | 性价比高的一步 | 中。单目已实测 121~171% CPU，双目约翻倍；且**与跟随的深度需求冲突（§3.6），要先定方案再做** |
| **⑦ `limited_navfn_planner` 从上游 navfn 重建** | 【已验证】它就是上游 `nav2_navfn_planner` 改名分支（package.xml 里 maintainer 还是 Steve Macenski），符号逐一对应，只多三个方法。**法律上干净**（Apache-2.0 传递） | 极低 |

---

### T2：数人月级（**8~14 人月**，重写闭源件）

| 交付物 | 为什么值得做 | 风险 |
|---|---|---|
| **① BSP 正规化**（17 个 stage 注入器转有序 patch series + `kernel-tree-mods` 重生成 + pristine 树重放 + 实机复现） | 这是本项目最有技术含量、最独家的资产。注入器 docstring 里的取证说明本身就是最有价值的文档 | **3 人月起。** 注入器锚点绑死 r35.6.4 具体源文件文本，第三方拿去别的小版本很可能失配；补丁全部包在 `r32_camera_power` 运行时门控里，门控语义必须讲清楚 |
| **② 视觉主链**（bbox 生产者 YOLO11n + `athena_tracking` 重写 + 集成节点） | 拿回"跟随"招牌功能。`athena_tracking` **零 AI 依赖**、协议已从二进制完整反推、二代 `cyberdog_vision` 是白送的 Apache-2.0 参考实现 | **1.5~2 人月，前置 trtexec 验证。** 盘点自评"2~3 人周"与它自己把集成节点评为"人月"直接矛盾 |
| **③ 导航 nav2 基线** | 算法层几乎 100% 上游开源 | **人月级起。** 缺的不是那 300 行胶水，还要在 Foxy 上编起 nav2 全栈（狗上那棵是小米 modified 的）、给 costmap 找传感器层、定位方案。目标定为**「能力对等即算成功」** |
| **④ 本地语音链** | 让狗能离线听懂话 | **拆两句**：保留小米 CDN 二进制（vpm/paddy/minerva）+ 修参考通道 = **人周**；彻底去小米（替 `libvpm` 6 麦 AEC/DOA/VAD/KWS）= **数人月+**。「人周级且不依赖小米任何东西」这两句不可能同时成立 |
| **⑤ 自研运动控制器落地** | 让开发者能跑自己的步态/RL 策略 | **1~2 人月，且被电池阻塞。** 官方 README 明说 1 代未充分测试；部署脚本会 kill 工厂控制器；**这不是"重建"，是拿官方自认未验证的代码替换一台会走路的机器的平衡控制器** |

**T2 明确不做**：移植二代 miloc（数人月+，一代硬件/发行版都不同，改用 rtabmap 自带定位模式即可）；逐行 faithful 复刻 move_base2（人月级且收益为负）；破解 Dreame 加密（既违规又无必要）。

---

### T3：需要社区／外部条件

| 事项 | 为什么值得做 | 先决条件 |
|---|---|---|
| **招募第二台狗（最低成本形式：发只读体检脚本让机主回传）** | **把 N 从 1 提上来。** 在此之前所有"高置信度"都应改写为「在单台设备上高置信度」。这件事的优先级应高于补第八个域 | 社区 |
| **GPL 索源（两个抓手）** | ①`open_vins` GPL-3：NOTICE.md 逐字写着 `License: GPL-3 / Usage: modified` ——**这是小米自己承认的义务，不需要争论事实**；②运动板跑 Allwinner TinaLinux（OpenWrt 系，大量 GPL 组件），**同性质的第二个抓手，此前没人注意到** | 社区发起，别抱期望，当免费选项 |
| **硬件文档：拆机测绘 / URDF / CAD** | 社区最实际的诉求是**维修**（3D 打印外壳与足垫），不是仿真 | 愿意拆机的志愿者 |
| **法律意见** | 再分发边界、逆向合法性（EU 2009/24/EC §6 vs 美国 DMCA §1201 vs 中国软件条例第十七条，辖区差异极大）、`athena.xlsm Rev 1.06` 与 `3rdparty.tar` 的授权状态 | 外部 |
| **zbwu R35 全刷路线验证** | 若成立，等于存在一条更彻底的 BSP 路线（argus + 硬件 ISP 直接可用） | **需要第二台愿意冒砖险的设备**，不要在这台上做 |
| **`3rdparty.tar` 可达性**（二代闭源 SDK，可能含 `detect.onnx`） | 可能一次补上视觉域唯一硬缺口 | 国内出口重试；**下载需机主明确同意；且只能作加速器，不能作依赖** |

---

## 5. 最小可行开源平台（MVP-OSS）定义

**如果只做一件事，那应该是：《CyberDog 一代开发者平台包（JP4 基线，不刷机）》。**

理由：它让一个拿到二手狗的开发者，在**不刷机、不冒砖险、不需要 x86 刷机主机**的前提下，就能读懂这台机器、安全地控制它、并写自己的 ROS2 节点。这是「完整二次开发」的**最小充分条件**，也是唯一能把入口门槛降到"有一台狗 + 一根 Type-C 线"的形态。

### 包含

1. **只读体检 + 全量备份工具**（`dogfingerprint.sh` / `dogcheck.sh` / `backup-all.sh`）——**强制第一步**，刷机相关内容一律放在备份章节之后
2. **《硬件功能级拓扑手册》**（DTB 反推 + 设备清单 + 电源域），明确标注"软件视角 ≠ 电气规格"
3. **协议与消息定义包**：28 条 LCM（含 `SE3VelocityCMD` 的 `NAVIGATOR=3` sourceid）、gRPC proto、蓝牙 GATT（服务 C420/写 C421/通知 C422）、`interaction_msgs` 语义表（`body`/`face`/`tracking_pose`/`robot_order`/`exe_monorder`）
4. **一个可编译的 `cyberdog_ros2` fork**（Foxy 回锁 + `cyberdog_vision` 恢复 + `scenedetection` 修复 + launch 分层：`lc_bringup` = 开箱即用最小闭环，`automation_launch` = 标注需要出厂二进制）
5. **开源 gRPC 控制端 CLI/库**（取代官方 App 的控制路径，**自带鉴权层**）
6. **缺件接口契约文档**：bbox 生产者接口、`athena_tracking` 输入输出契约、nav2↔`SE3VelocityCMD` 桥接契约——让社区能分头补，而不是等一个人补完
7. **闭源件清单表**：名称 / 真实身份 / 获取路径（原厂 URL + SHA256）/ ABI 约束（TRT7+CUDA10.2+sm_72）/ 是否可替换 / 替换方案 —— **只给表，不给二进制**
8. **事实勘误表**（§4-T0-③）
9. **安全三件套**：安全基线（"控制通道无鉴权，勿接不可信网络"）、运动实验安全规程、RECOVERY.md
10. **《开始之前你需要什么》** + 许可架构 + 维护承诺级别声明

### 不包含（且要写明为什么）

- ❌ **任何第三方二进制**：小米 CDN 的 5 个 tarball、rootfs 镜像、QSPI 内容、模型权重、NVIDIA 引导器 —— 镜像即再分发，法律性质与"官方 CMake 自己去拉"完全不同
- ❌ **`/params` 真实标定**（每台唯一、不可再生、对别人无意义）与 **`token.toml`/`token.json`**（机主小米账号凭据，**误发 = 泄露他人凭证**，须写进 CI 检查）
- ❌ **JP5 内核 BSP** —— 作为进阶分支单独发布，不进 MVP
- ❌ **出厂上层 ROS2 栈** —— 只给"从你自己设备的 `nvme0n1p1` 提取"的指引
- ❌ **可直接利用的攻击细节**（OTA 状态机字段级还原、PoC）—— 只给能力性描述 + 加固指引
- ❌ **任何驱动电机的教程放在安全规程之前**；且必须标注"电池是硬前置"

---

## 6. 给机主的建议：按投入产出比排序的前 3 件

### 第 1 件：**今天就做资产保全**（人日～人周，无先决条件）

四份不可再生/难再获得的资产**全部只存在于这一台 Mac 的本地目录里**【已验证：`git ls-files` 对 `/params`、`qspi-backup-factory.bin`、`build/mirror`、`cyberdog_ros2` 全部零命中】。
做法：`/params` tar+sha256 入库、32MB QSPI 入异地备份、`build/mirror` 七棵树（尤其 `athena_l4t_sdk.git` —— 刷机三件套的唯一来源）异地备份、`cyberdog_ros2` 改 submodule 锁 commit。
**这是唯一一件"不做就可能永久损失、且成本只有几小时"的事。** 比"小米 CDN 会下架"更近、更现实的单点风险。

### 第 2 件：**两个判决实验 + 一次拍板**（人日级，无先决条件）

- **一条 `trtexec --loadEngine`** → 决定整个视觉域（2.5~3 人月的投入）是否成立。在跑通之前，视觉域所有依赖 GPU 的结论**都不能算数**。
- **修 AEC 参考通道，然后说一次「旺财旺财」** → 决定语音域（人周 vs 数人月+）。这个基本事实至今零验证。⚠️ 链路一旦通，一句话直达电机，**必须机主在场**。
- **基线三方对照表并拍板**（JP4 原生 / JP5+chroot / zbwu 全刷）→ 不定这个，后面所有工时估算缺前提。**我倾向 JP4 基线做首发。**

**这三件加起来不到一周，但决定了 5~8 人月投入的方向。** 目前的路线图是建立在三个未验证假设上的。

### 第 3 件：**买电池 + 补许可架构**（各人日～人周，电池有采购周期）

电池解锁运动/导航/跟随/语音四个域的**全部验证能力**——现在这台狗物理上做不了任何一次运动验证，而运动域四条"已解决"无一例外必须驱动电机才能验证。
许可架构（分区多许可 + SPDX + NOTICE + DCO）是**公开发布的先决条件**，不是收尾工作。
两件都是"低技术含量但卡着一切"的类型。

> **不建议现在做的**：BSP 正规化（3 人月，但它服务的是 JP5 路线，而路线还没拍板）；重写 move_base2（等基线定了再说）；移植二代 miloc（数人月+，建议直接不做）。

---

## 7. 诚实的风险与不确定性

### 7.1 信心低的结论（不要拿它们做决策依据）

| 结论 | 当前标注 | 真实状态 |
|---|---|---|
| 「视觉域 feasibility 高 / confidence 高」 | 盘点给"高" | **应降为"未验证 / 中"**。整域建立在"R32 用户态 TRT 能在 R35 内核上跑"这个**从未执行过一次推理**的假设上。垫片只覆盖 2 个 ioctl，且**结构体大小不变、字段错位的 ABI 差异根本不会返回 ENOTTY**，垫片的诊断计数器天然抓不到这类问题。旁证：【已验证 13:16】GPU load 长期为 0 |
| 「NVDLA 完全闲置，可 `--useDLACore` 卸载」 | 盘点写进路线 | **属猜测**。R32 TRT7 的 DLA 路径在 R35 内核上是完全没碰过的第二块 ABI 面 |
| 「板上跑的就是 `cyberdog_locomotion` 这套开源码」 | 盘点当事实 | **应降为"共享同一 LCM 接口契约"**。固件加密，二进制层面零证据 |
| 「运动板固件满足 motor_sdk ≥1.0.0.94」 | 质疑 A 说可结案 | **维持未验证**（Jetson 整机版本 ≠ 运动板 robot-software 版本，两套体系） |
| 「BSP 产物 = 全部板载感知外设可用」 | 盘点承诺 | **不成立**。GPS/LED/触摸/超声/BMS 的**数据面跑在闭源出厂节点里**；干净 samplefs 上只剩"驱动在、没人产数据"。应改为「内核/DTB/驱动级可用」+ 一张"用户态数据面需另写"的表 |
| 「出厂语音播报能听见」 | 07-26 机主亲耳验证 | **已确认回归**。当前 pulse 只有 `auto_null` sink，card 1 没进 mi 用户的 pulseaudio |
| 「CPU idle 仅 3%」 | 盘点当实测 | **修正**：那是 top 首帧累计值。【本次复核 13:16】vmstat 三次采样 id 稳定在 32%，load average 11.2 → **空闲约 1.9 核，紧张但不是饱和**。结论方向（预处理放 GPU、检测按墙钟节流）不变，但数字要改 |
| 七个域的 confidence 全是"高" | — | **应统一改写为「在这台设备上高置信度」**。样本量 = 1 且是被前任改过的二手机 |

### 7.2 需要先验证什么（按信息量/成本比排序）

1. **`trtexec --loadEngine`** —— 一条命令，同时回答"TRT 活不活"和"三个 .engine 的确切维度"。视觉域 3 人月的入场券。
2. **修 AEC 参考通道 → 说一次唤醒词** —— 零写入零风险，决定语音域是人周还是数人月+。
3. **登板 `192.168.55.233` 只读确认** `/robot`、`/mnt/UDISK`、`user_code_ctrl_mode` 开关、`robot-software` 版本 —— 运动域全部路线的地基。
4. **电池兼容性** —— 换上第三方包后 BMS 握手/SOC 上报/充电协商是否正常。
5. **在 pristine r35.6.4 树上重放一遍 AE 注入流程** —— BSP 可信度最终取决于这一步（目前没人在第二台设备或全新环境里从 patch series 走通过一遍）。
6. **TF 树完整性**（`motion_manager` 是否真在广播 `odom→base_footprint`）—— 决定 `chassis_node` 是否真的冗余；误判会让 costmap 与 rtabmap **静默用错帧**。
7. **`192.168.55.100` 与 `.233` 同 MAC** —— 【已验证】运动板在 `l4tbr0` 网桥上（桥接 eth0 + rndis0 + usb0），这既解释了官方"Download 口传文件"的部署路径（好消息），也是暴露面（Download 口一插，外部机器与运动板 SSH 同二层）。这个同 MAC 的第二地址需要单独查清。

### 7.3 三条我不愿意粉饰的话

**① 这份成果同时是一份针对所有在网 CyberDog 的攻击手册。** gRPC 50051 明文无鉴权、OTA 10206 未发现包体签名校验、DDS 局域网开放、我们自建的服务以 root 自启，而厂商已停更、永远不会有补丁。发布必须按负责任披露的方式做：**能力性描述 + 加固指引优先，可直接利用的细节不发**；我们自己的产物默认不监听 `0.0.0.0`、默认不无鉴权、默认不启用 OTA 通道。

**② 发布之后的成本没人估过。** 【已验证】零 CI、零自动化测试、bus factor = 1。别人的狗版本不同，issue 会以"在我的狗上不工作"的形式涌进来。参照物就在眼前：官方仓自 2021-10-14 起近五年零代码提交，issue #162/#170/#174/#181 全部 Open 无人应答。**必须在 README 显式写明维护承诺级别**（best-effort、不接受硬件相关 issue、不承诺跨版本支持），否则会变成第二个无人应答的仓库。

**③ 一代的受众规模没人评估过。** 共创版限量 400 台、9999 元、二手稀缺；社区与厂商注意力都在二代；整套知识资产是中文的（`PORT-STATUS.md` 162KB 中文叙事型文档），而 GitHub 上的一代用户主要说英文。**如果结论是受众极小，那正确策略可能不是包装成"一代专用 SDK"，而是把方法论与工具链（内核补丁注入器、RCM 救砖流程、v4l2→ROS2 桥、L4T 跨代移植经验）作为通用 Jetson 移植资产发布** —— 那个受众大一个数量级。这个战略判断值得在投 10 人月之前先花一周想清楚。

---

## 附录：本次撰写期间的独立复核（全部只读）

| 项 | 结果 |
|---|---|
| 仓库根 LICENSE/COPYING/NOTICE | **零匹配**（质疑成立） |
| `git ls-files cyberdog_ros2` | **0**（.gitignore 第 10 行整个排除，质疑成立） |
| `build/qspi-backup-factory.bin` | 存在（33,554,432 字节），**未入库** |
| `build/mirror/` | 7 棵树（含 `athena_l4t_sdk.git`、`cyberdog_tegra_kernel.git`、`public_sources_r35.6.4.tbz2`），**未入库** |
| `kernel-tree-mods` 最后提交 | `7aecd66` **2026-07-26**，grep 相机 stage 标记 **零命中**（质疑成立） |
| 仓库规模 | 845 tracked files（build 663 / kernel-tree-mods 81 / dog-scripts 56 / tools 30 / laptop 4），24 个 stage 注入器，`PORT-STATUS.md` 2764 行 |
| 狗 `/sys/class/power_supply/` @13:16 | **只有 `usb-charger`，无 battery**（质疑成立，运动验证物理中断） |
| 狗 GPU load @13:16 | **0**（GPU 完全闲置成立） |
| 狗 CPU @13:16 | vmstat×3：`us 46 / sy 21-22 / **id 32**`，load average 11.21 → **约 1.9 核空闲**（修正盘点的"idle 3%"） |